
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Stores user profile information for the NEU Library Check-in app, including authentication details, role, and blockage status.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity (Firebase Authentication UID)."
        },
        "email": {
          "type": "string",
          "description": "User's institutional email address, strictly restricted to @neu.edu.ph domain.",
          "format": "email"
        },
        "displayName": {
          "type": "string",
          "description": "User's full name or preferred display name."
        },
        "role": {
          "type": "string",
          "description": "User's role in the system. Accepted values are 'admin' or 'user'."
        },
        "collegeId": {
          "type": "string",
          "description": "Reference to the College entity the user belongs to. (Relationship: College 1:N User)"
        },
        "visitorType": {
          "type": "string",
          "description": "Whether the user is a 'Student' or 'Employee (Faculty/Staff)'.",
          "enum": ["Student", "Employee (Faculty/Staff)"]
        },
        "isBlocked": {
          "type": "boolean",
          "description": "Indicates if the user is blocked from accessing the application. True means access is denied."
        }
      },
      "required": [
        "id",
        "email",
        "displayName",
        "role",
        "collegeId",
        "isBlocked"
      ]
    },
    "Visit": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Visit",
      "type": "object",
      "description": "Records individual entry logs for library visitors, including who visited, when, and their purpose.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Visit entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User entity who made the visit. (Relationship: User 1:N Visit)"
        },
        "timestamp": {
          "type": "string",
          "description": "Timestamp when the visit was recorded.",
          "format": "date-time"
        },
        "purposeOfVisit": {
          "type": "string",
          "description": "The specific reason for the user's visit to the library."
        },
        "collegeId": {
          "type": "string",
          "description": "Reference to the College entity the visitor belongs to at the time of visit. (Relationship: College 1:N Visit)"
        },
        "visitorType": {
          "type": "string",
          "description": "The type of visitor recorded at the time of visit (Student or Employee)."
        }
      },
      "required": [
        "id",
        "userId",
        "timestamp",
        "purposeOfVisit",
        "collegeId"
      ]
    },
    "College": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "College",
      "type": "object",
      "description": "Maintains a list of valid colleges, used for dropdown menus and filtering purposes.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the College entity."
        },
        "name": {
          "type": "string",
          "description": "The official name of the college."
        }
      },
      "required": [
        "id",
        "name"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles. The document ID `{userId}` is the Firebase Authentication UID. Includes denormalized 'role' and 'isBlocked' fields for user-specific data and admin management.",
          "params": [
            {
              "name": "userId",
              "description": "The unique Firebase Authentication UID of the user."
            }
          ]
        }
      },
      {
        "path": "/visits/{visitId}",
        "definition": {
          "entityName": "Visit",
          "schema": {
            "$ref": "#/backend/entities/Visit"
          },
          "description": "Records individual library entry logs. Includes denormalized 'userId' for authorization independence (allowing users to create their own logs without 'get()' calls) and 'collegeId' for admin analytics.",
          "params": [
            {
              "name": "visitId",
              "description": "The unique identifier for the visit log."
            }
          ]
        }
      },
      {
        "path": "/colleges/{collegeId}",
        "definition": {
          "entityName": "College",
          "schema": {
            "$ref": "#/backend/entities/College"
          },
          "description": "Master list of valid colleges. Accessible for read by all authenticated users for dropdown menus. Only admins can manage this collection.",
          "params": [
            {
              "name": "collegeId",
              "description": "The unique identifier for the college."
            }
          ]
        }
      },
      {
        "path": "/roles_admin/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Dedicated collection for explicit admin role-based access control. The document ID `{userId}` marks a user as an administrator. Used for robust, authorization-independent permission checks (e.g., via 'exists()' in rules).",
          "params": [
            {
              "name": "userId",
              "description": "The unique Firebase Authentication UID of the admin user."
            }
          ]
        }
      }
    ]
  }
}
